Сформируй черновики на основе [](<../Структура инструкций/_.md>).

**Список черновики:**

`style-context` — Общий контекст о форматах, разделах

meta.yaml:
```yaml
  conditions:
    - implicit_invocation
```

Все остальные черновики генерируются формируются [](<../Структура инструкций/_.md>) самостоятельно.

meta.yaml:
```yaml
  conditions:
    - explicit_invocation
```

content.md в начале:
```md
**Связанные правила:**
- @draft(style-context)
```
