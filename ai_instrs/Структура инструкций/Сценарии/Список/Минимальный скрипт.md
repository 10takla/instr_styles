**Общий шаблон:**
```md
**Входные данные:**
- <> – <>

**Задача:** Написать NodeJS скрипт для <>.

**Алгоритм:** <>

**Требования:**
- единый [](<../../ai_artifacts/<>.js>) без сторонних зависимостей (только NodeJS)
- кроссплатформенный
- пути к другим локальным файлам определяются относительно файла скрипта
- к выводу:
    - <[](#логирование)>
    - <[](#вывод-логов)>
- Прерывай сценарий и возвращай код `1` при ошибке, ненулевом коде агента или тайм-ауте; возвращай `0` при успехе.

**Выходные данные:**
- <>
```


## Другие требования

### Логирование:
```md
- Вывод итоговых данных в `stdout` в формате JSON.
- Вывод ошибок в `stderr`.
```

### Вывод логов:
```md
- **Промежуточное логирование (`--log_dir`):**
    * Выводить логи в `<path>.jsonl`
```

### Расапараллеливание:
```md
- Добавить аргумент `--concurrency <N>` для распараллеливания по <N> потокам
```
